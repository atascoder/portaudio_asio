# Где найти лог portaudio_asio_debug.log

## 📍 Местоположение файла лога

Лог файл будет создан в **текущей папке** там, где запущено приложение:

```
portaudio_asio_debug.log
```

Это может быть:
- 📁 Папка где лежит ваше приложение
- 📁 Папка где лежит `portaudio.dll` 
- 📁 Текущая рабочая папка Windows

## 🔍 Способы найти файл лога

### Способ 1: Самый простой - посмотреть в папку приложения

Если вы запустили приложение `MyApp.exe`, то:
1. Откройте папку где лежит `MyApp.exe`
2. Должен быть файл `portaudio_asio_debug.log`
3. Откройте его Блокнотом

### Способ 2: Через PowerShell

```powershell
# Найти все логи в текущей папке
Get-ChildItem -Name portaudio_asio_debug.log

# Показать содержимое лога
Get-Content portaudio_asio_debug.log

# Показать последние 50 строк
Get-Content portaudio_asio_debug.log -Tail 50

# Смотреть в реальном времени (как tail -f на Linux)
Get-Content portaudio_asio_debug.log -Wait -Tail 20
```

### Способ 3: Через CMD

```cmd
# Показать содержимое
type portaudio_asio_debug.log

# Показать последние 50 строк
powershell -Command "Get-Content portaudio_asio_debug.log -Tail 50"
```

### Способ 4: Прямой поиск

```powershell
# Найти все portaudio_asio_debug.log файлы в системе
Get-ChildItem -Recurse -Name portaudio_asio_debug.log -ErrorAction SilentlyContinue
```

## ⏰ Когда создается лог

- **Создается:** Когда вызывается `Pa_OpenStream()` в вашем приложении
- **Обновляется:** Каждый раз при создании нового потока (в режиме append)
- **Находится:** В текущей рабочей папке (Current Working Directory)

## 🚨 Если файл не создается

Возможные причины:

1. **DLL еще не обновлена** на новую версию с логированием
   - Используйте DLL из последнего коммита: `4fde248`

2. **Приложение не вызывает `Pa_OpenStream()`**
   - Убедитесь, что приложение действительно инициализирует PortAudio

3. **Недостаточно прав** на запись в текущую папку
   - Запустите приложение от администратора: **Ctrl + Shift + Enter**

4. **Текущая рабочая папка** не та, что вы думаете
   - Проверьте в PowerShell: `Get-Location`

5. **Антивирус блокирует запись**
   - Временно отключите или добавьте приложение в исключения

## 📊 Где создаст лог на Windows

Пример: если ваше приложение находится в `C:\Users\John\Music\MyApp\`

```
C:\Users\John\Music\MyApp\portaudio_asio_debug.log
```

Если запустили из PowerShell/CMD из другой папки, лог создадится ТАМ:

```powershell
cd C:\Programs
C:\Programs\MyApp.exe
# Лог создастся в C:\Programs\portaudio_asio_debug.log
```

## ✅ Проверить, что лог работает

1. **Откройте PowerShell** в папке приложения
2. **Запустите приложение:**
   ```powershell
   .\MyApp.exe
   ```
3. **Оставьте его работать 2-3 секунды**
4. **Откройте новое PowerShell окно** в той же папке
5. **Проверьте лог:**
   ```powershell
   Get-Content portaudio_asio_debug.log
   ```

## 🔄 Очистить лог перед следующим тестом

```powershell
# Удалить лог
Remove-Item portaudio_asio_debug.log

# Или очистить его содержимое
"" | Set-Content portaudio_asio_debug.log
```

---

**Главное:** Лог теперь создается в **текущей папке**, поэтому его легко найти! 📁

